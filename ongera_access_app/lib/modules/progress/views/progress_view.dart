import 'package:flutter/cupertino.dart';

class ProgressView extends StatelessWidget {
  const ProgressView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Progress screen in progress"));
  }
}
